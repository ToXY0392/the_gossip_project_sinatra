<html>
  <head>
    <meta charset="utf-8" />
    <title>THP Gossip News</title>

    <!-- Bootstrap -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet">

    <!-- Bootstrap Icons -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css"
      rel="stylesheet">

    <style>
      /* Bouton verre / ghost */
      .btn-ghost-potin {
        background: rgba(255, 255, 255, 0.08);
        color: #ffffff;
        border: 1px solid rgba(255, 255, 255, 0.9);
        border-radius: 999px;
        padding: 10px 32px;
        font-weight: 600;
        letter-spacing: 0.03em;
        text-shadow: 0 0 4px rgba(0, 0, 0, 0.4);
        box-shadow: 0 6px 16px rgba(0, 0, 0, 0.35);
        backdrop-filter: blur(8px);
        -webkit-backdrop-filter: blur(8px);
        transition: all 0.2s ease;
      }

      .btn-ghost-potin:hover {
        background: rgba(255, 255, 255, 0.22);
        color: #ffffff;
        box-shadow: 0 0 20px rgba(255, 255, 255, 0.65);
        transform: translateY(-1px);
      }

      /* Card verre / ghost */
      .gossip-card {
        background: rgba(255, 255, 255, 0.08);
        border: 1px solid rgba(255, 255, 255, 0.9);
        border-radius: 24px;
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.5);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        color: #ffffff;
      }

      .gossip-card .card-body {
        padding: 20px 24px;
      }

      /* Badge Potin */
      .badge-potin {
        font-size: 0.7rem;
        background: #00a2ff;
        color: #ffffff;
        padding: 3px 10px;
        border-radius: 999px;
        text-transform: uppercase;
        letter-spacing: 0.05em;
      }

      /* Contenu propre */
      .gossip-author {
        color: #ffffff;
        font-weight: 600;
        font-size: 1.2rem;
      }

      .gossip-content {
        color: #f2f2f2;
        font-size: 1.05rem;
      }

      /* Card "aucun potin" */
      .gossip-empty {
        background: rgba(255, 255, 255, 0.08);
        border: 1px solid rgba(255, 255, 255, 0.9);
        border-radius: 24px;
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.5);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        color: #ffffff;
      }
    </style>

  </head>

  <body style="background:#dedede; margin:0;">

    <!-- HEADER -->
    <header class="text-center py-5" style="background:#dedede; width:100%;">
      <h1 class="fw-bold mb-2"> THP Gossip News</h1>
      <p class="text-muted mb-0">
        Les rumeurs les plus inutiles, mais clairement les plus importantes.
      </p>
    </header>

    <!-- FOND AVEC IMAGE -->
    <div style="
      background-image: url('https://images.unsplash.com/photo-1522202176988-66273c2fd55f?auto=format&fit=crop&w=1600&q=80');
      background-size: cover;
      background-position: center;
      background-attachment: fixed;
      background-color: rgba(0,0,0,0.18);
      background-blend-mode: multiply;
      padding: 40px 0;
      min-height: 70vh;
    ">

      <div class="container" style="max-width: 800px;">

        <!-- BOUTON NOUVEAU POTIN -->
        <div class="text-center mb-4">
          <a href="/gossips/new/"
             class="btn-ghost-potin text-decoration-none">
            Nouveau potin
          </a>
        </div>

        <!-- LISTE DES POTINS -->
        <% if gossips.empty? %>

          <div class="gossip-empty text-center p-4">
            <p class="mb-1">Aucun potin pour le moment.</p>
            <p class="mb-0"><strong>Tout le monde est trop sage…</strong></p>
          </div>

        <% else %>

          <% gossips.each_with_index do |gossip, index| %>

            <div class="card gossip-card mb-4 border-0">
              <div class="card-body">

                <div class="d-flex justify-content-between align-items-start mb-2">

                  <div>
                    <span class="badge-potin mb-1">
                      Potin #<%= index + 1 %>
                    </span>
                    <h5 class="gossip-author mb-0">
                      <%= gossip.author %>
                    </h5>
                  </div>

                  <a href="/gossips/<%= index + 1 %>"
                     class="btn btn-sm btn-outline-light rounded-pill px-3">
                    Voir le potin
                  </a>
                </div>

                <p class="mt-2 mb-0 gossip-content">
                  <%= gossip.content %>
                </p>

              </div>
            </div>

          <% end %>

        <% end %>

      </div>
    </div>

    <!-- FOOTER -->
    <footer class="text-center py-4 text-muted"
            style="background:#dedede; margin:0;">

      <p class="mb-2">Retrouve THP Gossip News sur :</p>

      <div class="d-flex justify-content-center gap-3 flex-wrap mt-2">

        <a href="https://www.instagram.com/the_hacking_project"
           target="_blank"
           class="btn btn-sm text-white rounded-pill px-3"
           style="background: radial-gradient(circle at 30% 30%, #fdf497 0, #fdf497 5%, #fd5949 45%, #d6249f 60%, #285AEB 90%); border:none;">
          <i class="bi bi-instagram me-1"></i>
          Instagram
        </a>

        <a href="https://www.linkedin.com/school/the-hacking-project"
           target="_blank"
           class="btn btn-sm text-white rounded-pill px-3"
           style="background-color:#0A66C2; border:none;">
          <i class="bi bi-linkedin me-1"></i>
          LinkedIn
        </a>

        <a href="https://www.thehackingproject.org"
           target="_blank"
           class="btn btn-sm text-white rounded-pill px-3"
           style="background-color:#444; border:none;">
          <i class="bi bi-globe me-1"></i>
          Site web
        </a>

      </div>
    </footer>

  </body>
</html>

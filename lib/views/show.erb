<html>
  <head>
    <meta charset="utf-8" />
    <title>THP Gossip News - Potin <%= id %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet">
  </head>

  <body class="bg-light">

    <div class="container py-5" style="max-width: 800px;">
      <h1 class="mb-4 text-center">Potin #<%= id %></h1>

      <div class="card shadow-sm mb-4">
        <div class="card-body">
          <h5 class="card-title mb-2"><strong>Auteur :</strong> <%= gossip.author %></h5>
          <p class="card-text mb-3"><strong>Contenu :</strong><br><%= gossip.content %></p>

          <div class="d-flex gap-2">
            <!-- LIEN IMPORTANT : /gossips/:id/edit/ -->
            <a href="/gossips/<%= id %>/edit/" class="btn btn-warning">
              Modifier ce potin
            </a>

            <a href="/" class="btn btn-secondary">
              Retour à la liste
            </a>

            <%# Si tu as une suppression dans l'exo %>
            <%# 
            <form action="/gossips/<%= id %>/delete/" method="post" class="ms-auto">
              <button type="submit" class="btn btn-danger"
                      onclick="return confirm('Supprimer ce potin ?');">
                Supprimer
              </button>
            </form>
            %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
